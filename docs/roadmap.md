# Roadmap

## Next improvements

- Add TLS-enabled Apache profile with local certificate automation.
- Add optional Prometheus + Grafana observability profile.
- Add encrypted backup export workflow to object storage.
- Add disaster-recovery drill script with timing output.
- Add split Compose profiles (`core`, `ops`, `observability`).

## Documentation extensions

- Host-level Linux tuning guide (ulimits, sysctl, disk IO checks).
- Apache tuning notes for concurrency and timeout strategy.
- More failure drills in runbooks (network partitions, disk pressure).
